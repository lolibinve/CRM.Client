<UserControl x:Class="CRM.Modular.Views.RoleManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CRM.Modular.Views"
             xmlns:cm="http://caliburnmicro.com"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBox" >
                <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
                <Setter Property="Width" Value="100"></Setter>
                <Setter Property="Height" Value="30"></Setter>
            </Style>
            <Style TargetType="TextBlock" >
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
            </Style>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/CRM.Modular;component/BaseThemes.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <!--<TextBlock Text="员工姓名：" />
            <TextBox Width="100"/>-->
            <Button x:Name="Delete" Margin="0,0,0,0" Width="80" Content="删 除"/>
            <Button x:Name="Add" Margin="10,0,0,0" Width="80" Content="新 增"/>
            <Button x:Name="Query" Margin="10,0,0,0" Width="80" Content="查 询" />
        </StackPanel>

        <DataGrid x:Name="OrderLst" Grid.Row="1" AutoGenerateColumns="False"
                  LoadingRow="Lst_LoadingRow" RowHeight="60"
                  CanUserAddRows="False"
                  SelectedItem="{Binding SelectData}" IsReadOnly="True"
                  cm:Message.Attach="
                  [Event MouseDoubleClick] = [Action OrderLst_MouseDoubleClick($source,$eventArgs)]"
                  VerticalAlignment="Top" >
                <DataGrid.Columns>
                <DataGridTemplateColumn Header="选中" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox  Style="{StaticResource MaterialDesignAccentCheckBox}" 
                                       IsThreeState="False" 
                                       cm:Message.Attach="[Event Click] = [Action OrderItem_CheckedClick($source,$eventArgs)]"
                                       IsChecked="{Binding IsCheck,UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="序号" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type DataGridRow}}, Path=Header}" HorizontalAlignment="Left" VerticalAlignment="Center" ></TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="姓名" Width="100" Binding="{Binding Name,UpdateSourceTrigger=PropertyChanged}" ElementStyle="{x:Null}"/>
                <DataGridTextColumn Header="密码" Width="100" Binding="{Binding PassWord ,UpdateSourceTrigger=PropertyChanged}" ElementStyle="{x:Null}"/>
                <DataGridTextColumn Header="管理权限" Width="100" Binding="{Binding Admin ,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource PowerConverter}}" ElementStyle="{x:Null}"/>
                <DataGridTextColumn Header="SKU标签" Width="100" Binding="{Binding SkuTag ,UpdateSourceTrigger=PropertyChanged}" ElementStyle="{x:Null}"/>

            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</UserControl>
