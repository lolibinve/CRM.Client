<Window x:Class="CRM.Modular.Views.ListExpertDesignView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM.Modular.Views"
        mc:Ignorable="d"
        Title="导出" Height="400" Width="500" AllowDrop="False"  WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">

    <Grid Margin="20,0,20,0">
        <Grid.RowDefinitions>
            <RowDefinition  Height="*"/>
            <RowDefinition  Height="60"/>
        </Grid.RowDefinitions>

        <Grid Row="0" Margin="10,10,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <TextBlock Text="数据导出类型:" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" Margin="5"  Grid.ColumnSpan="2" Height="16" Width="75"/>
                <RadioButton x:Name="Rb"  Grid.Row="0"  Grid.Column="1"  Margin="50,0,0,0" Content="Update" IsChecked="{Binding IsUpdate}"></RadioButton>
                <RadioButton x:Name="Rb2"  Grid.Row="0" Grid.Column="2" Content="Delete" Margin="50,0,0,0" IsChecked="{Binding IsUpdate,Converter={StaticResource InvertBooleanConverter}}" />
            </StackPanel>


            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="品牌:"  Grid.Row="2" HorizontalAlignment="Left"  Margin="5,5,0,5" Grid.Column="0" />
                <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding BrandName}" Padding="0,0,0,3" Style="{StaticResource MaterialDesignTextBox}"  Margin="10,5,5,5" />
            </Grid>


            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="分类:" Grid.Row="2" Grid.Column="0"  Margin="5,5,0,5"/>
                <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding RecommendedBrowseNodes}"  Padding="0,0,0,3" Style="{StaticResource MaterialDesignTextBox}"  Margin="10,5,5,5"/>
            </Grid>


            <!--<TextBlock Text="商品条码导出类型:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
            <TextBox Grid.Row="1"  Margin="5" Grid.Column="1" Text="{Binding UPCPar}"></TextBox>-->

            <ScrollViewer Grid.Row="3" Margin="0,20,0,5" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Top" HorizontalAlignment="Center">
                <ListBox  x:Name="CountriesListBox" ItemsSource="{Binding Countriess}"  Style="{StaticResource MaterialDesignFilterChipPrimaryOutlineListBox}">
                </ListBox>
            </ScrollViewer>

        </Grid>

        <Button x:Name="Export"  Grid.Row="1" Grid.Column="1" Width="80" Height="35" Content="导出"  />
    </Grid>
</Window>
