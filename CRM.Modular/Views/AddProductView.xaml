<Window x:Class="CRM.Modular.Views.AddProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM.Modular.Views"
        xmlns:t="clr-namespace:Aipark.Wpf.Controls;assembly=Aipark.Wpf.Controls"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="455" Width="800" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">

    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
                <Setter Property="Height" Value="50"></Setter>
                <Setter Property="Margin" Value="10,0,0,0"></Setter>
                <Setter Property="Foreground" Value="#828282"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
                <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
                <Setter Property="HorizontalAlignment" Value="Right"></Setter>
                <Setter Property="Foreground" Value="#A7A7A7"></Setter>
            </Style>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/CRM.Modular;component/BaseThemes.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>


    <Grid Margin="0,30,0,0" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition />
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="45"/>
            <RowDefinition Height="45"/>
            <RowDefinition Height="45"/>
            <RowDefinition Height="45"/>
            <RowDefinition Height="45"/>
            <RowDefinition Height="45"/>
            <RowDefinition Height="65.24"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SKU:" Grid.Column="0"/>
        <TextBox Grid.Column="2" Text="{Binding product.Sku}" VerticalAlignment="Center" Padding="0,0,0,3" Style="{StaticResource MaterialDesignTextBox}" Height="20"/>

        <TextBlock Text="业务员:" Grid.Row="1"  Grid.Column="0" />
        <TextBlock Grid.Column="2" Grid.Row="1" Text="{Binding product.Operator}"  VerticalAlignment="Center" Padding="0,0,0,3" HorizontalAlignment="Left"  Height="20"/>

        <TextBlock Text="添加日期:" Grid.Column="0" Grid.Row="2"/>
        <TextBlock Grid.Column="2" Grid.Row="2" Text="{Binding product.Date,StringFormat='{}{0:yyyy-MM-dd}'}" VerticalAlignment="Center" HorizontalAlignment="Left"  Padding="0,0,0,3"  Height="20"/>

        <!--<TextBlock Text="图片:" Grid.Column="0"  Grid.Row="3"/>-->

        <Border Grid.Column="4"  Grid.Row="1" Grid.RowSpan="4" BorderBrush="Gray"  BorderThickness="1">
            <Image Width="120" Height="120"  Source="{Binding product.ImageBase64Str,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource Base64ImageConverter}}" >
            </Image>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="4" Width="100" >
            <Button  Name="AddPic" Content="上传图片" Height="35" Width="100" Style="{StaticResource MaterialDesignFlatLightBgButton}"/>
        </StackPanel>

        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="5" Width="100" >
            <TextBlock  Text="图片链接预览" Height="auto" Width="120" FontSize="14" IsEnabled="False" HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Center" />
        </StackPanel>
        <Border Grid.Column="5"  Grid.Row="1" Grid.RowSpan="4" BorderBrush="Gray"  BorderThickness="1" Margin="5,0,0,0" >
            <Image Width="120" Height="120"  Source="{Binding product.ImageUrl,UpdateSourceTrigger=PropertyChanged}" >
            </Image>
        </Border>

        <TextBlock Text="备注:" Grid.Row="3" Grid.Column="0"/>
        <TextBox Grid.Column="2" Grid.Row="3" Text="{Binding product.Remark}"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 Style="{StaticResource MaterialDesignFilledTextBox}" Grid.RowSpan="2"/>

        <TextBlock Text="图片链接:" Grid.Row="5" Grid.Column="0"/>
        <TextBox Grid.Column="2" Grid.ColumnSpan="4" Grid.Row="5" Text="{Binding product.ImageUrl}" VerticalAlignment="Center" Padding="0,0,0,3" Style="{StaticResource MaterialDesignTextBox}" Height="20"/>


        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="8" Grid.ColumnSpan="6" Grid.Column="0" Margin="0,0,0,20" Width="250">
            <Button Name="CloseForm" Content="取消" Height="35" Width="100"/>
            <Button Name="Sure" Content="确认" Height="35" Width="100" Margin="50,0,0,0" />
        </StackPanel>

        <Grid Grid.Row="0" Grid.RowSpan="7" Grid.ColumnSpan="6">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsProgressIndeterminate}" Value="True">
                            <Setter Property="Background" Value="#55eaeaea"/>
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsProgressIndeterminate}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <t:ProgressRing Grid.Row="0" Grid.RowSpan="4" HorizontalAlignment="Center" VerticalAlignment="Center" IsIndeterminate="{Binding IsProgressIndeterminate}"/>
        </Grid>

    </Grid>
</Window>
