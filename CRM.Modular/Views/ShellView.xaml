<Window x:Class="CRM.Client.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cm="http://caliburnmicro.com"
        xmlns:local="clr-namespace:CRM.Client.Views"
        mc:Ignorable="d"
        Icon="/CRM.Modular;component/ymx.ico"
        Height="800" Width="1700" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Window.Resources>
        <Style x:Key="btn_nap" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"></ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Window.Template>
        <ControlTemplate TargetType="{x:Type Window}">
            <Border BorderBrush="gray" BorderThickness="0">
                <DockPanel x:Name="DockPanel">
                    <ContentPresenter x:Name="content" />
                </DockPanel >
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="WindowState" Value="Maximized">
                    <Setter TargetName="DockPanel" Property="Margin" Value="8" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Template>
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="35"
            CornerRadius="0"
            GlassFrameThickness="1"
            NonClientFrameEdges="None"
            ResizeBorderThickness="4"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="85"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Background="#339999">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Image Source="/CRM.Modular;component/ymx.ico"  Margin="10,5,0,0"/>
                <TextBlock x:Name="lblTitle" Text="管理系统" Foreground="White" FontSize="24" Margin="5,0,20,5" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                <Button WindowChrome.IsHitTestVisibleInChrome="True" Click="btn_MiniSize" Content="─" Style="{StaticResource btn_nap}" HorizontalAlignment="Right" Foreground="White" Margin="0 0 5 0" Height="30" Width="30"/>
                <Button WindowChrome.IsHitTestVisibleInChrome="True" Click="btn_MaxSize" Content="☐" Style="{StaticResource btn_nap}" Foreground="White" Height="30" Width="30" RenderTransformOrigin="0.556,0.267"/>
                <Button WindowChrome.IsHitTestVisibleInChrome="True" Click="btn_Close" Content="✕" Style="{StaticResource btn_nap}" Foreground="White" Margin="0 0 0 0" Height="30" Width="30" RenderTransformOrigin="0.178,0.894"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Background="#EDEDED">
            <ContentControl cm:View.Model="{Binding ActiveItem}"/>
        </Grid>
    </Grid>
</Window>
