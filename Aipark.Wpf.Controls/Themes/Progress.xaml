<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Aipark.Wpf.Controls">

    <Style TargetType="{x:Type local:ProgressRing}">
        <Setter Property="ImageSource" Value="pack://application:,,,/Aipark.Wpf.Controls;component/Resources/Images/loading.png"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#096dd9"/>
        <Setter Property="Width" Value="120"/>
        <Setter Property="Height" Value="120"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ProgressRing}">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <Image Source="{TemplateBinding ImageSource}" Stretch="Uniform"
                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               RenderTransformOrigin="0.5,0.5">
                            <Image.RenderTransform>
                                <RotateTransform x:Name="rotateTransform" Angle="0"/>
                            </Image.RenderTransform>
                        </Image>
                        <TextBlock Text="{TemplateBinding Progress}" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}"
                                   FontWeight="{TemplateBinding FontWeight}" Foreground="{TemplateBinding Foreground}" TextAlignment="Center" TextWrapping="Wrap"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsIndeterminate" Value="true"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard x:Name="rotateStory">
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation From="0" To="360" Duration="00:00:03" Storyboard.TargetName="rotateTransform" Storyboard.TargetProperty="Angle" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsIndeterminate" Value="false"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Visibility" Value="Collapsed" />
                            <MultiTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="rotateStory"/>
                            </MultiTrigger.EnterActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>