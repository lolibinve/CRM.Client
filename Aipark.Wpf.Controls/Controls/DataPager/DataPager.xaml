<UserControl x:Class="Aipark.Wpf.Controls.DataPager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Aipark.Wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="22" />
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="9,0,0,0"/>
        </Style>

        <Style TargetType="{x:Type Button}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="9,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <TextBlock x:Name="textBlock" VerticalAlignment="Stretch" Text="{TemplateBinding Content}" HorizontalAlignment="Stretch" Cursor="Hand" Foreground="Blue"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" TargetName="textBlock" Value="#FFABA5A5"/>
                                <Setter Property="Cursor" Value="None"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <TextBlock FontFamily="Microsoft YaHei UI" VerticalAlignment="Center">
            <Run Text="共"/> 
            <Run Text="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=TotalCount}"/>
            <Run Text="行，每页"/>
            <Run Text="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=PageSize}"/>
            <Run Text="行，第"/>
            <Run Text="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=PageIndex}"/>
            <Run Text="/"/>
            <Run Text="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=PageCount}"/>
            <Run Text="页"/>
        </TextBlock>

        <Button Content="首页" IsEnabled="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=CanGoFirstOrPrev}" 
                Click="GoToFirstPage_Click"/>
        <Button Content="上一页" IsEnabled="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=CanGoFirstOrPrev}" 
                Click="GoToPreviousPage_Click"/>
        <Button Content="下一页" IsEnabled="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=CanGoLastOrNext}"  
                Click="GoToNextPage_Click"/>
        <Button Content="末页" IsEnabled="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=CanGoLastOrNext}" 
                Click="GoToLastPage_Click"/>
        <TextBox x:Name="inputPageIndexTbx" Text="{Binding RelativeSource={RelativeSource Mode= FindAncestor,AncestorType={x:Type local:DataPager}},Path=PageIndex,Mode=OneWay}"/>
        <Button Content="转到" Click="GoToInputPageIndex_Click" />
    </StackPanel>
</UserControl>
