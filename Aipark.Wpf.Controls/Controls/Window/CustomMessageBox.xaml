<local:TWindow x:Class="Aipark.Wpf.Controls.CustomMessageBox"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:local="clr-namespace:Aipark.Wpf.Controls"
               mc:Ignorable="d"
               ResizeMode="NoResize" Topmost="True"
               WindowStartupLocation="CenterScreen"
               ShowInTaskbar="False"
               Title="系统提示" MinHeight="160" MinWidth="260"
               Width="260" Height="160">

    <Window.Resources>
        <ResourceDictionary>
            <RoutedUICommand x:Key="OkButton"/>
            <RoutedUICommand x:Key="CancelButton"/>
        </ResourceDictionary>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource OkButton}" Executed="OkButton_Click"/>
        <CommandBinding Command="{StaticResource CancelButton}" Executed="CancelButton_Click"/>
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Key="Space" Command="{StaticResource OkButton}"/>
        <KeyBinding Key="Esc" Command="{StaticResource CancelButton}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="msgTextBlock" Text="我是消息提示文字" FontSize="13" TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="18,0,12,0"/>
        <Border Grid.Row="1" Background="WhiteSmoke" BorderBrush="Gainsboro" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="OkButton" Content="确定" Width="80" Height="22" Click="OkButton_Click" IsDefault="True" Margin="0,0,12,0" TabIndex="1"/>
                <Button x:Name="YesButton" Content="是" Width="80" Height="22" Click="YesButton_Click" Margin="0,0,12,0"/>
                <Button x:Name="NoButton" Content="否" Width="80" Height="22" Click="NoButton_Click" Margin="0,0,12,0"/>
                <Button x:Name="CancelButton" Content="取消" Width="80" Height="22" Click="CancelButton_Click" Margin="0,0,12,0" TabIndex="0"/>
            </StackPanel>
        </Border>
    </Grid>
</local:TWindow>
