<UserControl x:Class="Aipark.Wpf.Controls.FileBrowserBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="150" d:DesignHeight="24" >
    <Grid>
        <TextBox HorizontalAlignment="Stretch" Height="24" Padding="2,2,26,2" IsReadOnly="True" Text="{Binding SelectedPath}"/>
        <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,2,0"
                Style="{DynamicResource GeometryFindButtonStyle}" Click="FindButton_Click"/>
    </Grid>
</UserControl>
