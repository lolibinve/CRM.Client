<UserControl x:Class="Aipark.Wpf.Controls.DropdownComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cvt="clr-namespace:Aipark.Wpf.Converters"
             xmlns:local="clr-namespace:Aipark.Wpf.Controls"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="200">

    <UserControl.Resources>
        <cvt:DisplayMemberConverter x:Key="DisplayMemberConverter"/>

        <DataTemplate x:Key="ListBoxItemDataTemplate">
            <TextBlock >
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource DisplayMemberConverter}">
                        <Binding />
                        <Binding Path="DisplayMemberPath" RelativeSource="{RelativeSource AncestorType={x:Type local:DropdownComboBox}}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </DataTemplate>

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="4,1"/>
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Bd" Height="24" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                <Image x:Name="image" Source="pack://application:,,,/Aipark.Wpf.Controls;component/Resources/Images/check.png" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="Gainsboro"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="WhiteSmoke"/>
                                <Setter Property="Visibility" TargetName="image" Value="Visible"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="True"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="WhiteSmoke"/>
                                <Setter Property="Visibility" TargetName="image" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <TextBox x:Name="selectedTbx" local:TextBoxHelper.Watermark="{Binding Watermark}" 
                 Background="White" Foreground="Black" BorderThickness="1" 
                 VerticalContentAlignment="Center" Padding="2,2,8,2"  IsReadOnly="True"
                 Text="{Binding Text}"/>
        <Button HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource DropdownArrowButtonStyle}" Click="QueryButton_Click"/>

        <Popup x:Name="itemsPopup" PopupAnimation="Fade" PlacementTarget="{Binding ElementName=selectedTbx}" 
               Placement="{Binding Placement}" AllowsTransparency="True" StaysOpen="False" IsOpen="False" VerticalOffset="1"
               Width="{Binding ElementName=selectedTbx,Path=ActualWidth}">
            <Border x:Name="itemsBorder" Background="White" BorderBrush="Silver" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <ListBox x:Name="listBox" BorderThickness="0" SelectionMode="{Binding SelectionMode}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                         ItemsSource="{Binding ItemsSource}" ItemTemplate="{StaticResource ListBoxItemDataTemplate}" SelectionChanged="ListBox_SelectionChanged">
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>
