<UserControl x:Class="Aipark.Wpf.Controls.SuperImage.ImageLoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Aipark.Wpf.Controls.SuperImage"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Storyboard x:Key="ProgressStoryboard" RepeatBehavior="Forever">
            <DoubleAnimation From="0" To="360" Duration="00:00:03" Storyboard.TargetName="rotateTransform" Storyboard.TargetProperty="Angle" />
        </Storyboard>

        <Style TargetType="{x:Type Hyperlink}">
            <Setter Property="Foreground" Value="DimGray"/>
            <Setter Property="TextBlock.TextDecorations" Value="None"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image x:Name="image" Grid.Row="0" Source="pack://application:,,,/Aipark.Wpf.Controls;component/Resources/Images/loading2.png" Stretch="None"
               HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" Visibility="Collapsed">
            <Image.RenderTransform>
                <RotateTransform x:Name="rotateTransform" Angle="0"/>
            </Image.RenderTransform>
        </Image>

        <TextBlock x:Name="textBlock" Grid.Row="1" FontFamily="Consolas" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
            <Hyperlink Click="Hyperlink_Click">
                <Run x:Name="textBlockRun" Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type local:ImageLoadingControl}},Path=Fault}"/>
            </Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
